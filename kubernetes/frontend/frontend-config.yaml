apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: fullstackapp
data:
  # Replace keys and values with your frontend config
  API_URL: "http://backend-service.fullstackapp.svc.cluster.local:8080"
  FEATURE_FLAG: "true"
  # Add other config variables your frontend needs
